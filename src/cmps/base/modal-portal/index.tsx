import { FC, ReactNode } from "react"
import { BookersModal } from "../bookers-modal"

interface Props {
}

export const ModalPortal: FC<Props> = ({ }) => {
    return <div id="modal-portal">
    </div>
}